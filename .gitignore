# Secrets — never commit
secrets/

# Terraform local state and artifacts (terraform/infra-base/, terraform/environments/*/, ...)
terraform/**/.terraform/
terraform/**/terraform.tfstate
terraform/**/terraform.tfstate.backup
terraform/**/terraform.tfstate.*.backup
terraform/**/tfplan.out
terraform/**/*.tfplan
terraform/**/crash.log
terraform/**/.pre-env-split-backup/

# Ansible
*.retry

# Environment-specific config (real IPs/paths) — commit the .example versions instead
ansible/inventory/hosts.ini
ansible/group_vars/all/hosts.yml
# terraform.tfvars agora e versionado (ver commit "Versionar terraform.tfvars"):
# so tem IPs de rede privada e chaves SSH publicas, nada sensivel de verdade,
# e precisa estar no git pra mudancas de infra virarem PR revisavel.
